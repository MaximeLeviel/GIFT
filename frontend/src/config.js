//TODO: Change URLs
const config = {
  apiBaseUrl: {
    development: "http://127.0.0.1:8080/gift-1.0-SNAPSHOT/api",
    // The staging and production envs will likely to be different in real scenario
    staging: "http://127.0.0.1:8080/gift-1.0-SNAPSHOT/api",
    production: "http://localhost:8080/api",
  },
};

export default config;
